#!/usr/bin/env node
/**
 * Dedicated demo Supabase project seeding was removed.
 * The /demo route uses in-browser localStorage data instead.
 */
console.error(
  "seed-demo-supabase.mjs is disabled: use the public /demo sandbox instead of seeding a demo Supabase project.",
);
process.exit(1);
