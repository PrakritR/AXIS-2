#!/usr/bin/env node
/**
 * Production @axis.local demo DB seeding was removed.
 * Use the public /demo sandbox (no login) for interactive exploration instead.
 */
console.error(
  "seed-production-portal.mjs is disabled: production demo accounts are no longer seeded.\n" +
    "Explore the product at /demo on any deployment.",
);
process.exit(1);
