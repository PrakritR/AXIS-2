---
description: Keeper branch cursor-2 — all commits land here
alwaysApply: true
---

# Local agent branch (cursor-2)

You are in the **cursor-2** sandbox worktree.

- Commit and push to branch **`cursor-2`** only (no remote feature branches).
- Full-stack test: **http://localhost:3011** (`bin/fm-proplane-open-localhost.sh cursor-2` from firstmate home).
- Integration testing happens on **`main`** (port 3000) after captain approval.
  (`prakrit` was retired Jul 25, 2026 — the ladder is `main` → `production`.)
