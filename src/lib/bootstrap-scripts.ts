/** Inline boot scripts for root layout — keep as plain strings for next/script. */

export const THEME_BOOTSTRAP_SCRIPT = `(function(){try{var p=location.pathname||'/';if(!/^\\/(portal|resident|admin|vendor|auth)(\\/|$)/.test(p)){document.documentElement.setAttribute('data-theme','light');return;}var t=localStorage.getItem('axis:theme');if(t!=='light'&&t!=='dark'){t='light';}document.documentElement.setAttribute('data-theme',t);}catch(e){document.documentElement.setAttribute('data-theme','light');}})();`;

export const CAPACITOR_BOOTSTRAP_SCRIPT = `(function(){try{var c=window.Capacitor;if(c&&c.isNativePlatform&&c.isNativePlatform()){var p=c.getPlatform&&c.getPlatform();document.documentElement.setAttribute('data-native',p==='android'?'android':'ios');var vp=document.querySelector('meta[name="viewport"]');if(vp)vp.setAttribute('content','width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover');var hide=function(){try{var m=c.Plugins&&c.Plugins.SplashScreen;if(m&&m.hide)m.hide();}catch(e){}};hide();window.addEventListener('load',hide);setTimeout(hide,2500);var u=new URL(location.href);if((u.pathname==='/'||u.pathname==='')&&(u.searchParams.get('code')||u.searchParams.get('error'))){u.pathname='/auth/callback';location.replace(u.pathname+u.search+u.hash);}}}catch(e){}})();`;
