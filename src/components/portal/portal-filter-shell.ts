/** Residents-tab reference layout for a single property (or compact) filter trigger. */
export const PORTAL_PROPERTY_FILTER_SHEET_CLASS =
  "min-w-0 shrink-0 max-md:w-full max-md:[&_button]:w-full max-md:[&_button]:px-2.5";

/** Multi-field filter sheets (Communication, Payments, Screenings, Documents library). */
export const PORTAL_MULTI_FIELD_FILTER_SHEET_CLASS = PORTAL_PROPERTY_FILTER_SHEET_CLASS;
